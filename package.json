{
  "name": "notecli",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run -w @note-research/note-core build && npm run -w note-research-cli build",
    "test": "npm run -w note-research-cli test",
    "lint": "npm run -w note-research-cli lint"
  },
  "devDependencies": {
    "@types/node": "^25.3.0",
    "typescript": "^5.9.3"
  }
}
